import { HTMLFrame } from './HTMLFrame'

export default {
  title: '5. Components for emails/Frame/HTML Frame',
  render: () => HTMLFrame('This is everything you need to set up an HTML Email. It contains all header tags, etc. Start by copying the markup of this component.'),
}

export const Default = {}
